from django . shortcuts import render from djexperiments . models import Experiment from djPsych . utils import fetch_files_of_type def home ( request ) :      available = Experiment . objects . all ( ) return render ( request , <str> , { <str> : available } )  def allExperiments ( request ) :      return render ( request , <str> )  def index ( request ) :      return render ( request , <str> )    