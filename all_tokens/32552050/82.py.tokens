import re from django . core . exceptions import ValidationError from django . utils . translation import ugettext_lazy as _ def validate_exp_label ( label ) :      if not re . match ( <str> , label ) :          raise ValidationError ( _ ( <str> ) )    