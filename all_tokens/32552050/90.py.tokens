from django import template from djcollect . models import Participation register = template . Library ( ) @ register . inclusion_tag ( <str> ) def cardify ( experiment , request ) :      is_researcher = experiment . is_researcher ( request ) if experiment . get_latest_pending ( request ) and experiment . enforce_finish :          force_cont = True  else :          force_cont = False  return { <str> : experiment , <str> : is_researcher , <str> : force_cont }  @ register . inclusion_tag ( <str> ) def results ( experiment , length ) :      parts = experiment . participation_set . all ( ) . order_by ( <str> ) [ : length ] return { <str> : parts , <str> : experiment }   