import glob import os . path from django . conf import settings def get_all_js_files_in_exp ( exp_label , path = <str> ) :      base_path = settings . BASE_DIR + <str> + exp_label files = [ ] for file in glob . glob ( os . path . join ( base_path , path ) + <str> ) :          files . append ( os . path . basename ( file ) )  return files  def fetch_files_of_type ( pathname , ext ) :      if not pathname . endswith ( <str> ) :          pathname = pathname + <str>  location = os . path . dirname ( os . path . join ( settings . BASE_DIR , <str> , pathname ) ) files = [ ] for file in glob . glob ( location + <str> + ext ) :          files . append ( os . path . basename ( file ) )  return files  def get_type ( filepath ) :      image_ext = ( <str> , <str> , <str> , <str> , <str> , <str> , <str> ) audio_ext = ( <str> , <str> , <str> , <str> ) video_ext = ( <str> , ) extension = os . path . splitext ( filepath ) [ 1 ] if extension . lower ( ) == <str> :          return <str>  elif extension . lower ( ) == <str> :          return <str>  elif extension . lower ( ) in image_ext :          return <str>  elif extension . lower ( ) in audio_ext :          return <str>  elif extension . lower ( ) in video_ext :          return <str>  else :          return <str>    