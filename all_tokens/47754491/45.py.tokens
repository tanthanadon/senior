from datetime import datetime import time def test ( ) : 	 global hasrun print ( <str> . format ( datetime . now ( ) ) ) time . sleep ( 5 ) hasrun = True  year , month , day , hour , minute = 2016 , 12 , 23 , 15 , 55 now = datetime . now ( ) print <str> , now jobstart = datetime ( year , month , day , hour , minute ) jobstop = datetime ( year , month , day , hour , minute + 1 ) print <str> , jobstart print <str> , jobstop print datetime . now ( ) - jobstart hasrun = False while True : 	 while ( ( datetime . now ( ) > jobstart ) and ( datetime . now ( ) < jobstop ) ) : 		 run = True test ( )  else : 		 print ( <str> . format ( datetime . now ( ) ) ) run = False if hasrun : 			 minute = minute + 2 jobstart = datetime ( year , month , day , hour , minute ) jobstop = datetime ( year , month , day , hour , minute + 1 ) print <str> , jobstart print <str> , jobstop hasrun = False  time . sleep ( 10 )    