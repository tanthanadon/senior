from evdev import InputDevice , list_devices from pprint import pprint for dev in list_devices ( ) :     device = InputDevice ( dev ) pprint ( ( <str> , device , <str> , device . name ) ) cap = device . capabilities ( verbose = True , absinfo = True ) pprint ( ( <str> , cap ) )   