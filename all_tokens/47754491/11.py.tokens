import cv2 cap = cv2 . VideoCapture ( 0 ) img_counter = 0 while ( True ) :      ret , frame = cap . read ( ) cv2 . imshow ( <str> , frame ) k = cv2 . waitKey ( 1 ) if k % 256 == 27 :          print ( <str> ) break  elif k % 256 == 32 :          img_name = <str> . format ( img_counter ) cv2 . imwrite ( img_name , frame ) print ( <str> . format ( img_name ) ) img_counter += 1   cap . release ( ) cv2 . destroyAllWindows ( )  