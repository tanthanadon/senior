from kodidb import MoreRequests def get_movies_by_imdb ( imdb ) : 	 db = MoreRequests ( ) res = db . get_movies_by_imdb ( imdb ) def movie ( item ) : 		 from log import debug debug ( unicode ( item ) ) return { <str> : item [ <str> ] , <str> : item [ <str> ] , <str> : item [ <str> ] , <str> : item [ <str> ] , <str> : item [ <str> ] , <str> : item [ <str> ] , <str> : item [ <str> ] }  movies = [ movie ( item ) for item in res ] return { <str> : movies }   