from xoutil . keywords import getkwd , setkwd , kwd_getter , kwd_setter , org_kwd def test_keywords ( ) :      class Foobar :          pass  proper = lambda v : isinstance ( v , type ) and issubclass ( v , Foobar ) obj = Foobar ( ) names = { <str> , <str> , <str> , <str> , <str> } for name in names :          setkwd ( obj , name , type ( name . title ( ) , ( Foobar , ) , { } ) )  for name , value in vars ( obj ) . items ( ) :          if proper ( value ) :              assert org_kwd ( name ) . title ( ) == value . __name__   kwd_setter ( obj ) ( <str> , 123 ) assert obj . if_ is kwd_getter ( obj ) ( <str> ) assert obj . and_ is getkwd ( obj , <str> ) assert obj . xyz is getkwd ( obj , <str> ) assert obj . else_ is getkwd ( obj , <str> ) keys = { org_kwd ( n ) for n , v in vars ( obj ) . items ( ) if proper ( v ) } assert keys == names   