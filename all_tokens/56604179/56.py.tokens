from textwrap import * import textwrap as _stdlib from textwrap import __all__ __all__ = list ( __all__ ) def dedent ( text , skip_firstline = False ) :      <str> if skip_firstline :          parts = text . split ( <str> , 1 ) if len ( parts ) > 1 :              subject , body = parts  else :              subject , body = parts [ 0 ] , <str>  result = _stdlib . dedent ( subject ) if body :              result += <str> + _stdlib . dedent ( body )   else :          result = _stdlib . dedent ( text )  return result   