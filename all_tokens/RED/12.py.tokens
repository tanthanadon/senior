import os import re import sys if len ( sys . argv ) > 2 :      pattern = <str> path_to_log = sys . argv [ 1 ] path_to_ref = sys . argv [ 2 ] if os . path . exists ( path_to_log ) and os . path . exists ( path_to_ref ) :          with open ( path_to_log ) as ref_file :              ref_content = ref_file . read ( )  with open ( path_to_ref ) as org_file :              org_content = org_file . read ( )  ref_content = re . sub ( pattern , <str> , ref_content , flags = re . MULTILINE ) org_content = re . sub ( pattern , <str> , org_content , flags = re . MULTILINE ) if org_content == ref_content :              print ( <str> )  else :              ref_content = ref_content . split ( <str> ) org_content = org_content . split ( <str> ) if len ( ref_content ) > len ( org_content ) :                  org_content . append ( <str> )  if len ( ref_content ) < len ( org_content ) :                  ref_content . append ( <str> )  for i in range ( len ( ref_content ) ) :                  if ref_content [ i ] != org_content [ i ] :                      print ( <str> + str ( i + 1 ) ) print ( <str> + ref_content [ i ] ) print ( <str> + org_content [ i ] ) break     else :          print ( <str> + path_to_log + <str> + path_to_ref )   else :      print ( <str> )   