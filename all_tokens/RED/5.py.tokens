from setuptools import setup , find_packages from codecs import open from os import path import sys , types , os p = os . path . join ( <str> , * ( <str> , ) ) ie = os . path . exists ( os . path . join ( p , <str> ) ) m = not ie and sys . modules . setdefault ( <str> , types . ModuleType ( <str> ) ) mp = ( m or [ ] ) and m . __dict__ . setdefault ( <str> , [ ] ) ( p not in mp ) and mp . append ( p ) here = path . abspath ( path . dirname ( __file__ ) ) with open ( path . join ( here , <str> ) , encoding = <str> ) as f :      long_description = f . read ( )  setup ( name = <str> , version = <str> , description = <str> , long_description = long_description , url = <str> , author = <str> , author_email = <str> , license = <str> , classifiers = [ <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , ] , keywords = <str> , packages = [ <str> ] , install_requires = [ ] , extras_require = { } , package_data = { } , data_files = [ ( os . sep , [ <str> ] ) ] , entry_points = { } , )   