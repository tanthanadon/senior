import os import sys if len ( sys . argv ) > 2 :      if os . path . exists ( sys . argv [ 1 ] ) and os . path . exists ( sys . argv [ 2 ] ) :          status = [ ] f = open ( sys . argv [ 1 ] , <str> ) result_file = f . readlines ( ) f . close ( ) f = open ( sys . argv [ 2 ] , <str> ) schema_file = f . readlines ( ) f . close ( ) if result_file and schema_file :              counter = len ( schema_file ) if len ( result_file ) > len ( schema_file ) :                  status . append ( <str> . format ( str ( len ( result_file ) ) , str ( len ( schema_file ) ) ) ) counter = len ( schema_file )  if len ( schema_file ) > len ( result_file ) :                  status . append ( <str> . format ( str ( len ( schema_file ) ) , str ( len ( result_file ) ) ) ) counter = len ( resul_file )  for i in range ( counter ) :                  if result_file [ i ] != schema_file [ i ] :                      status . append ( <str> . format ( str ( i + 1 ) ) ) break   else :                  if status :                      status . append ( <str> . format ( str ( counter + 1 ) ) )  else :                      status . append ( <str> )   print ( <str> . join ( status ) )   else :          print ( <str> + sys . argv [ 1 ] + <str> + sys . argv [ 2 ] )   else :      print ( <str> )   