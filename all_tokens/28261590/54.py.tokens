from hicexplorer import chicViewpoint from tempfile import NamedTemporaryFile , mkdtemp import os import pytest import warnings warnings . simplefilter ( action = <str> , category = RuntimeWarning ) warnings . simplefilter ( action = <str> , category = PendingDeprecationWarning ) ROOT = os . path . join ( os . path . dirname ( os . path . dirname ( os . path . abspath ( __file__ ) ) ) , <str> ) def are_files_equal ( file1 , file2 , delta = 1 , skip = 0 ) :      equal = True if delta :          mismatches = 0  with open ( file1 ) as textfile1 , open ( file2 ) as textfile2 :          for i , ( x , y ) in enumerate ( zip ( textfile1 , textfile2 ) ) :              if i < skip :                  continue  if x != y :                  if delta :                      mismatches += 1 if mismatches > delta :                          equal = False break   else :                      equal = False break     return equal  def test_two_matrices ( ) :      outfile = NamedTemporaryFile ( suffix = <str> , delete = False ) output_folder = mkdtemp ( prefix = <str> ) outfile . close ( ) args = <str> . format ( ROOT + <str> , ROOT + <str> , ROOT + <str> , ROOT + <str> , 200000 , 200000 , output_folder , 1 ) . split ( ) chicViewpoint . main ( args ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert set ( os . listdir ( ROOT + <str> ) ) == set ( os . listdir ( output_folder ) )  def test_one_matrix ( ) :      outfile = NamedTemporaryFile ( suffix = <str> , delete = False ) output_folder = mkdtemp ( prefix = <str> ) outfile . close ( ) args = <str> . format ( ROOT + <str> , ROOT + <str> , ROOT + <str> , 200000 , 200000 , output_folder , 1 ) . split ( ) chicViewpoint . main ( args ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert set ( os . listdir ( ROOT + <str> ) ) == set ( os . listdir ( output_folder ) )  def test_two_matrices_writeFileName ( ) :      outfile = NamedTemporaryFile ( suffix = <str> , delete = False ) outfile_name_list = NamedTemporaryFile ( suffix = <str> , delete = False ) output_folder = mkdtemp ( prefix = <str> ) outfile . close ( ) args = <str> . format ( ROOT + <str> , ROOT + <str> , ROOT + <str> , ROOT + <str> , 200000 , 200000 , output_folder , outfile_name_list . name , 1 ) . split ( ) chicViewpoint . main ( args ) assert are_files_equal ( ROOT + <str> , outfile_name_list . name ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert set ( os . listdir ( ROOT + <str> ) ) == set ( os . listdir ( output_folder ) )  def test_one_matrix_writeFileName ( ) :      outfile = NamedTemporaryFile ( suffix = <str> , delete = False ) outfile_name_list = NamedTemporaryFile ( suffix = <str> , delete = False ) output_folder = mkdtemp ( prefix = <str> ) outfile . close ( ) args = <str> . format ( ROOT + <str> , ROOT + <str> , ROOT + <str> , 200000 , 200000 , output_folder , outfile_name_list . name , 1 ) . split ( ) chicViewpoint . main ( args ) assert are_files_equal ( ROOT + <str> , outfile_name_list . name ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert are_files_equal ( ROOT + <str> , output_folder + <str> , skip = 4 ) assert set ( os . listdir ( ROOT + <str> ) ) == set ( os . listdir ( output_folder ) )   