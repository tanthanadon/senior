import warnings warnings . simplefilter ( action = <str> , category = RuntimeWarning ) warnings . simplefilter ( action = <str> , category = PendingDeprecationWarning ) import pytest from tempfile import NamedTemporaryFile import os from psutil import virtual_memory import hicexplorer . hicAggregateContacts mem = virtual_memory ( ) memory = mem . total / 2 ** 30 LOW_MEMORY = 2 MID_MEMORY = 7 HIGH_MEMORY = 200 REMOVE_OUTPUT = True ROOT = os . path . join ( os . path . dirname ( os . path . dirname ( os . path . abspath ( __file__ ) ) ) , <str> ) matrix = ROOT + <str> BED = ROOT + <str> BED2 = ROOT + <str> outfile_aggregate_plots = NamedTemporaryFile ( suffix = <str> , prefix = <str> , delete = False ) diagnosticHeatmapFile = NamedTemporaryFile ( suffix = <str> , prefix = <str> , delete = False ) @ pytest . mark . skipif ( MID_MEMORY > memory , reason = <str> ) @ pytest . mark . parametrize ( <str> , [ matrix ] ) @ pytest . mark . parametrize ( <str> , [ outfile_aggregate_plots ] ) @ pytest . mark . parametrize ( <str> , [ BED ] ) @ pytest . mark . parametrize ( <str> , [ <str> ] ) @ pytest . mark . parametrize ( <str> , [ BED2 ] ) @ pytest . mark . parametrize ( <str> , [ 30 ] ) @ pytest . mark . parametrize ( <str> , [ <str> , <str> , <str> , <str> ] ) @ pytest . mark . parametrize ( <str> , [ <str> , <str> ] ) @ pytest . mark . parametrize ( <str> , [ <str> ] ) @ pytest . mark . parametrize ( <str> , [ <str> ] ) @ pytest . mark . parametrize ( <str> , [ diagnosticHeatmapFile ] ) @ pytest . mark . parametrize ( <str> , [ 4 ] ) @ pytest . mark . parametrize ( <str> , [ 4 ] ) @ pytest . mark . parametrize ( <str> , [ <str> , <str> , <str> ] ) @ pytest . mark . parametrize ( <str> , [ <str> ] ) @ pytest . mark . parametrize ( <str> , [ <str> ] ) @ pytest . mark . parametrize ( <str> , [ <str> , <str> ] ) @ pytest . mark . parametrize ( <str> , [ 0.01 ] ) @ pytest . mark . parametrize ( <str> , [ 1.0 ] ) def test_aggregate_contacts_two ( capsys , matrix , outFileName , BED , ran , BED2 , numberOfBins , transform , avgType , outFilePrefixMatrix , outFileContactPairs , diagnosticHeatmapFile , kmeans , hclust , howToCluster , chromosomes , colorMap , plotType , vMin , vMax ) :      args = <str> <str> <str> <str> <str> . format ( matrix , outFileName . name , BED , ran , BED2 , numberOfBins , transform , avgType , outFileContactPairs , kmeans , hclust , howToCluster , chromosomes , colorMap , plotType , vMin , vMax ) . split ( ) hicexplorer . hicAggregateContacts . main ( args ) os . remove ( outFileName . name )   