import warnings warnings . simplefilter ( action = <str> , category = RuntimeWarning ) warnings . simplefilter ( action = <str> , category = PendingDeprecationWarning ) from hicexplorer import hicCorrelate from tempfile import NamedTemporaryFile import os from matplotlib . testing . compare import compare_images ROOT = os . path . join ( os . path . dirname ( os . path . dirname ( os . path . abspath ( __file__ ) ) ) , <str> ) def test_correlate ( ) :      outfile_heatmap = NamedTemporaryFile ( suffix = <str> , prefix = <str> , delete = False ) outfile_scatter = NamedTemporaryFile ( suffix = <str> , prefix = <str> , delete = False ) args = <str> <str> <str> . format ( ROOT + <str> , ROOT + <str> , outfile_heatmap . name , outfile_scatter . name ) . split ( ) hicCorrelate . main ( args ) res = compare_images ( ROOT + <str> + <str> , outfile_heatmap . name , tol = 40 ) assert res is None , res res = compare_images ( ROOT + <str> + <str> , outfile_scatter . name , tol = 40 ) assert res is None , res os . remove ( outfile_heatmap . name ) os . remove ( outfile_scatter . name )  def test_correlate_chromosomes ( ) :      outfile_heatmap = NamedTemporaryFile ( suffix = <str> , prefix = <str> , delete = False ) outfile_scatter = NamedTemporaryFile ( suffix = <str> , prefix = <str> , delete = False ) args = <str> <str> <str> <str> . format ( ROOT + <str> , ROOT + <str> , outfile_heatmap . name , outfile_scatter . name ) . split ( ) hicCorrelate . main ( args ) res = compare_images ( ROOT + <str> + <str> , outfile_heatmap . name , tol = 40 ) assert res is None , res res = compare_images ( ROOT + <str> + <str> , outfile_scatter . name , tol = 40 ) assert res is None , res os . remove ( outfile_heatmap . name ) os . remove ( outfile_scatter . name )   