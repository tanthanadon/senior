__author__ = <str> import os def run ( DMSO , CA , genes , TSS , END , filedir ) :      os . system ( <str> + genes + <str> + DMSO + <str> + filedir + <str> ) os . system ( <str> + TSS + <str> + DMSO + <str> + filedir + <str> ) os . system ( <str> + END + <str> + DMSO + <str> + filedir + <str> ) os . system ( <str> + genes + <str> + CA + <str> + filedir + <str> ) os . system ( <str> + TSS + <str> + CA + <str> + filedir + <str> ) os . system ( <str> + END + <str> + CA + <str> + filedir + <str> )   