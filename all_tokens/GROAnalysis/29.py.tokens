__author__ = <str> def run ( DMSO , CA , filedir ) : 	 list1 = list ( ) with open ( DMSO ) as F1 : 		 for line in F1 : 			 chrom , start , stop , coverage = line . strip ( ) . split ( ) if float ( coverage ) < 0 : 				 coverage = - float ( coverage )  else : 				 coverage = float ( coverage )  list1 . append ( ( chrom , start , stop , coverage ) )   outfile1 = open ( filedir + <str> , <str> ) for item in list1 : 		 outfile1 . write ( item [ 0 ] + <str> + item [ 1 ] + <str> + item [ 2 ] + <str> + str ( item [ 3 ] ) + <str> )  list1 = list ( ) with open ( CA ) as F2 : 		 for line in F2 : 			 chrom , start , stop , coverage = line . strip ( ) . split ( ) if float ( coverage ) < 0 : 				 coverage = - float ( coverage )  else : 				 coverage = float ( coverage )  list1 . append ( ( chrom , start , stop , coverage ) )   outfile2 = open ( filedir + <str> , <str> ) for item in list1 : 		 outfile2 . write ( item [ 0 ] + <str> + item [ 1 ] + <str> + item [ 2 ] + <str> + str ( item [ 3 ] ) + <str> )     