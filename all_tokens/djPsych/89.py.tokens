from django . conf . urls import url , include from django . contrib import admin from djmanager . views import index from django . conf import settings from djexperiments . experiments . simcat import texture_generator from django . conf . urls . static import static import allauth . urls urlpatterns = [ url ( <str> , include ( <str> ) ) , url ( <str> , admin . site . urls ) , url ( <str> , include ( <str> , namespace = <str> ) ) , url ( <str> , include ( <str> ) ) , url ( <str> , include ( allauth . urls ) ) , url ( <str> , texture_generator , name = <str> ) , url ( <str> , index , name = <str> ) , ] if settings . IS_PRODUCTION is False :      additional = [ ] + static ( settings . MEDIA_URL , document_root = settings . MEDIA_ROOT ) urlpatterns . extend ( additional )   