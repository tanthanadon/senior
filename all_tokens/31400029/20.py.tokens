from __future__ import absolute_import from django . conf import settings from django . conf . urls import url from django . contrib . auth . decorators import login_required from django . views . static import serve from django . contrib . auth . views import LoginView from django . contrib . auth . views import LogoutView from django . contrib . auth . views import PasswordResetView from django . contrib . auth . views import PasswordResetCompleteView from django . contrib . auth . views import PasswordResetConfirmView from django . contrib . auth . views import PasswordResetDoneView urlpatterns = ( url ( <str> , LoginView . as_view ( template_name = <str> ) , name = <str> ) , url ( <str> , LogoutView . as_view ( template_name = <str> ) , name = <str> ) , url ( <str> , PasswordResetView . as_view ( template_name = <str> ) , name = <str> ) , url ( <str> , PasswordResetDoneView . as_view ( template_name = <str> ) , name = <str> ) , url ( <str> , PasswordResetConfirmView . as_view ( template_name = <str> ) , name = <str> ) , url ( <str> , PasswordResetCompleteView . as_view ( template_name = <str> ) , name = <str> ) , url ( <str> , login_required ( serve ) , { <str> : settings . STATIC_ROOT , <str> : <str> } ) , url ( <str> , serve , { <str> : settings . STATIC_ROOT } ) , )  