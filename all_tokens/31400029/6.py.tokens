from __future__ import absolute_import import os from ddtrace import patch_all patch_all ( ) from celery import Celery from django . conf import settings os . environ . setdefault ( <str> , <str> ) app = Celery ( <str> ) app . conf . ONCE = settings . CELERY_ONCE app . config_from_object ( <str> , namespace = <str> ) app . autodiscover_tasks ( ) @ app . task ( bind = True ) def debug_task ( self ) :      print ( <str> . format ( self . request ) )   