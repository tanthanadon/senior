from django . conf . urls import include from django . conf . urls import url from django . contrib import admin from django . conf import settings from django . conf . urls . static import static urlpatterns = [ url ( <str> , admin . site . urls ) , url ( <str> , include ( <str> ) ) , url ( <str> , include ( <str> ) ) , url ( <str> , include ( <str> ) ) , ] + static ( settings . STATIC_URL , document_root = settings . STATIC_ROOT )  