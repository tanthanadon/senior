import numpy as np import cv2 videoFilename = <str> cap = cv2 . VideoCapture ( videoFilename ) while ( cap . isOpened ( ) ) :      ret , frame = cap . read ( ) gray = cv2 . cvtColor ( frame , cv2 . COLOR_BGR2GRAY ) cv2 . imshow ( <str> , gray ) if cv2 . waitKey ( 1 ) & 0xFF == ord ( <str> ) :          break   cap . release ( ) cv2 . destroyAllWindows ( )  