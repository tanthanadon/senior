import os import sys import glob from jaratoolbox import settings mouseName = str ( sys . argv [ 1 ] ) fileName = <str> copyToDir = <str> + fileName + <str> + mouseName + <str> if not os . path . exists ( copyToDir ) :      os . makedirs ( copyToDir )  mainDir = settings . EPHYS_PATH + mouseName + <str> for multiFolder in glob . glob ( mainDir + <str> ) :      for clusterReport in glob . glob ( multiFolder + <str> ) :          clusterDay = clusterReport . split ( <str> ) [ 1 ] clusterNum = clusterReport . split ( <str> ) [ - 1 ] newFilename = copyToDir + mouseName + <str> + clusterDay + <str> + clusterNum os . system ( <str> + clusterReport + <str> + newFilename ) print newFilename    