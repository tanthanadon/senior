import matplotlib . pyplot as plt from math import log10 regWormFrac = 0.05 mutWormFrac = 0.5 mutPercent = 1.0 / 10000.0 wormScreeningRate = 1.0 plotRangeLayers = range ( 0 , 9 ) numLayer = 4 finalMutPercent = 1000 finalFrac = [ 0 ] * len ( plotRangeLayers ) for layer , indLayer in enumerate ( plotRangeLayers ) :      finalFrac [ indLayer ] = mutPercent * ( mutWormFrac / regWormFrac ) ** layer  plt . clf ( ) plt . plot ( plotRangeLayers , finalFrac ) plt . show ( ) finalFrac_numLayer = mutPercent * ( mutWormFrac / regWormFrac ) ** numLayer numLayerNeeded = log10 ( finalMutPercent / mutPercent ) / log10 ( mutWormFrac / regWormFrac ) totalWormScreen = 1.0 / ( mutPercent * ( mutWormFrac ** numLayer ) ) totalScreenTime = totalWormScreen / wormScreeningRate totalScreenTimeHour = totalScreenTime / 60.0 totalScreenTimeDay = totalScreenTimeHour / 24.0 print <str> . format ( numLayer , finalFrac_numLayer ) print <str> . format ( finalMutPercent , numLayerNeeded ) print <str> . format ( totalWormScreen ) print <str> . format ( totalScreenTime , totalScreenTimeHour , totalScreenTimeDay )  