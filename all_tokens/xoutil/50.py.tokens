def suffix_kwd ( name ) :      from keyword import iskeyword return <str> . format ( name ) if iskeyword ( name ) else name  def org_kwd ( name ) :      from keyword import iskeyword if name . endswith ( <str> ) :          res = name [ : - 1 ] return res if iskeyword ( res ) else name  else :          return name   def getkwd ( obj , name , default = None ) :      return getattr ( obj , suffix_kwd ( name ) , default )  def setkwd ( obj , name , value ) :      setattr ( obj , suffix_kwd ( name ) , value )  def delkwd ( obj , name ) :      delattr ( obj , suffix_kwd ( name ) )  def kwd_getter ( obj ) :      from functools import partial return partial ( getkwd , obj )  def kwd_setter ( obj ) :      from functools import partial return partial ( setkwd , obj )  def kwd_deleter ( obj ) :      from functools import partial return partial ( delkwd , obj )   