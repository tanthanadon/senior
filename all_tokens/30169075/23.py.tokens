import pytest from escpos . capabilities import get_profile , NotSupported , BARCODE_B , Profile @ pytest . fixture def profile ( ) :      return get_profile ( <str> )  class TestBaseProfile :      def test_get_font ( self , profile ) :          with pytest . raises ( NotSupported ) :              assert profile . get_font ( <str> )  assert profile . get_font ( 1 ) == 1 assert profile . get_font ( <str> ) == 0  def test_supports ( self , profile ) :          assert not profile . supports ( <str> ) assert profile . supports ( BARCODE_B )  def test_get_columns ( self , profile ) :          assert profile . get_columns ( <str> ) > 5 with pytest . raises ( NotSupported ) :              assert profile . get_columns ( <str> )    class TestCustomProfile :      def test_columns ( self ) :          assert Profile ( columns = 10 ) . get_columns ( <str> ) == 10  def test_features ( self ) :          assert Profile ( features = { <str> : True } ) . supports ( <str> )    