from __future__ import print_function import six import sys from escpos import printer from escpos . constants import CODEPAGE_CHANGE , ESC , CTL_LF , CTL_FF , CTL_CR , CTL_HT , CTL_VT def main ( ) :      dummy = printer . Dummy ( ) dummy . hw ( <str> ) for codepage in sys . argv [ 1 : ] or [ <str> ] :          dummy . set ( height = 2 , width = 2 ) dummy . _raw ( codepage + <str> ) print_codepage ( dummy , codepage ) dummy . _raw ( <str> )  dummy . cut ( ) print ( dummy . output )  def print_codepage ( printer , codepage ) :      if codepage . isdigit ( ) :          codepage = int ( codepage ) printer . _raw ( CODEPAGE_CHANGE + six . int2byte ( codepage ) ) printer . _raw ( <str> )  else :          printer . charcode ( codepage )  sep = <str> printer . set ( font = <str> ) printer . _raw ( <str> . format ( sep . join ( map ( lambda s : hex ( s ) [ 2 : ] , range ( 0 , 16 ) ) ) ) ) printer . set ( ) for x in range ( 0 , 16 ) :          printer . set ( font = <str> ) printer . _raw ( <str> . format ( hex ( x ) [ 2 : ] ) ) printer . set ( ) for y in range ( 0 , 16 ) :              byte = six . int2byte ( x * 16 + y ) if byte in ( ESC , CTL_LF , CTL_FF , CTL_CR , CTL_HT , CTL_VT ) :                  byte = <str>  printer . _raw ( byte ) printer . _raw ( sep )  printer . _raw ( <str> )   if __name__ == <str> :      main ( )   