from __future__ import print_function import matplotlib . pyplot as plt import numpy as np import os import tempfile from odl . contrib . mrc import ( FileReaderMRC , FileWriterMRC , mrc_header_from_params ) file_path = os . path . join ( os . path . dirname ( os . path . abspath ( __file__ ) ) , <str> , <str> ) with FileReaderMRC ( file_path ) as reader :      header , data = reader . read ( ) print ( <str> , reader . data_shape ) print ( <str> , reader . data_dtype ) print ( <str> , reader . data_axis_order ) print ( <str> , reader . header_size ) print ( <str> ) print ( <str> ) for label in reader . labels :          if label . strip ( ) :              print ( repr ( label ) )   print ( <str> )  values = np . zeros ( [ 10 , 20 , 30 ] , dtype = <str> ) values [ : , 5 : 15 , 20 : 25 ] = 1 print ( <str> , np . array_equal ( values , data ) ) plt . figure ( ) plt . title ( <str> ) plt . imshow ( data [ 5 ] , cmap = <str> , interpolation = <str> ) plt . show ( ) new_data = np . ones ( ( 50 , 100 , 200 ) , dtype = <str> ) header = mrc_header_from_params ( new_data . shape , new_data . dtype , kind = <str> ) out_file = tempfile . TemporaryFile ( ) with FileWriterMRC ( out_file , header ) as writer :      writer . write ( new_data ) print ( <str> <str> . format ( writer . file . seek ( 0 , 2 ) , writer . header_size , new_data . nbytes ) )   