import odl import odl . contrib . shearlab space = odl . uniform_discr ( [ - 1 , - 1 ] , [ 1 , 1 ] , [ 128 , 128 ] ) op = odl . contrib . shearlab . ShearlabOperator ( space , num_scales = 2 ) phantom = odl . phantom . shepp_logan ( space , True ) y = op ( phantom ) y . show ( <str> ) z = op . inverse ( y ) z . show ( <str> ) z = op . adjoint ( y ) z . show ( <str> )  