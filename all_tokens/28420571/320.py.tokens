import matplotlib . pyplot as plt import odl import numpy as np space = odl . uniform_discr ( 0 , 5 , 100 ) elem = space . element ( np . sin ) fig = elem . show ( title = <str> ) fig = ( elem / 2 ) . show ( fig = fig ) plt . show ( ) elem . show ( force_show = True )  