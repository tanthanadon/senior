from himlarcli . parser import Parser from himlarcli . printer import Printer from himlarcli . foremanclient import ForemanClient parser = Parser ( ) parser . toggle_show ( <str> ) parser . set_default_format ( <str> ) options = parser . parse_args ( ) printer = Printer ( options . format ) foreman = ForemanClient ( options . config , options . debug ) hosts = options . hosts mapping = { } for host in hosts :      fact = foreman . get_fact ( host , options . fact ) mapping [ host ] = fact  printer . output_dict ( mapping )  