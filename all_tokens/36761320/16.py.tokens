import sys import utils from himlarcli . nova import Nova from himlarcli . keystone import Keystone from himlarcli . mail import Mail from email . mime . text import MIMEText options = utils . get_options ( <str> , dry_run = True , hosts = False ) keystone = Keystone ( options . config , debug = options . debug ) mail = Mail ( options . config , debug = options . debug ) region = keystone . region print <str> sys . exit ( 0 ) subject = <str> % region body_file = <str> with open ( body_file , <str> ) as body_txt :      body_content = body_txt . read ( )  projects = keystone . list_projects ( <str> ) for project in projects :      msg = MIMEText ( body_content ) msg [ <str> ] = subject if not options . dry_run :          mail . send_mail ( project , msg )  print <str> % project  mail . close ( )  