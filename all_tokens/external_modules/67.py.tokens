from openerp import api , fields , models , _ class AccountPeriodClose ( models . TransientModel ) :      _inherit = <str> @ api . model def _get_sequence_id ( self ) :          periods = self . env [ <str> ] . browse ( self . _context . get ( <str> , [ ] ) ) if not periods :              raise Warning ( _ ( <str> ) )  elif len ( periods . mapped ( <str> ) ) > 1 :              raise Warning ( _ ( <str> ) )  sequence = self . env [ <str> ] . search ( [ ( <str> , <str> , <str> ) , ( <str> , <str> , periods [ 0 ] . company_id . id ) , ] , limit = 1 ) if not sequence :              sequence = sequence . search ( [ ( <str> , <str> , <str> ) ] , limit = 1 )  return sequence  sequence_id = fields . Many2one ( <str> , <str> , domain = [ ( <str> , <str> , <str> ) ] , context = { <str> : <str> } , help = <str> , default = _get_sequence_id , ) next_number = fields . Integer ( related = <str> , readonly = True , )   