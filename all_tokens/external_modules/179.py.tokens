from openerp import models , api class StockPicking ( models . Model ) :      _inherit = <str> @ api . multi def action_open_landed_cost ( self ) :          self . ensure_one ( ) line_obj = self . env [ <str> ] lines = line_obj . search ( [ ( <str> , <str> , self . id ) ] ) if lines :              mod_obj = self . env [ <str> ] model , action_id = tuple ( mod_obj . get_object_reference ( <str> , <str> ) ) action = self . env [ model ] . browse ( action_id ) . read ( ) [ 0 ] ids = set ( [ x . distribution . id for x in lines ] ) if len ( ids ) == 1 :                  res = mod_obj . get_object_reference ( <str> , <str> ) action [ <str> ] = [ ( res and res [ 1 ] or False , <str> ) ] action [ <str> ] = list ( ids ) [ 0 ]  else :                  action [ <str> ] = <str> % list ( ids )  return action     