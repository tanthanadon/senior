def init_actions_ ( service , args ) :      return { <str> : [ <str> ] }  def test ( job ) :      import sys import os import time RESULT_OK = <str> RESULT_FAILED = <str> RESULT_ERROR = <str> % job . service . name model = job . service . model model . data . result = RESULT_OK % job . service . name failures = [ ] repos = [ ] try :          expected_nr_of_jobs = 1 curdir = os . getcwd ( ) j . atyourservice . reposDiscover ( ) repo = j . atyourservice . repoGet ( j . sal . fs . joinPaths ( j . dirs . codeDir , <str> ) ) repos . append ( repo ) bp_path = j . sal . fs . joinPaths ( repo . path , <str> , <str> ) repo . blueprintExecute ( path = bp_path ) start_time = time . time ( ) os . chdir ( repo . path ) job . service . executor . execute ( <str> ) end_time = time . time ( ) nr_of_jobs = len ( j . core . jobcontroller . db . jobs . find ( actor = <str> , service = <str> , action = <str> , fromEpoch = start_time , toEpoch = end_time ) ) if nr_of_jobs != expected_nr_of_jobs :              failures . append ( <str> % ( expected_nr_of_jobs , nr_of_jobs ) )  if failures :              model . data . result = RESULT_FAILED % <str> . join ( failures )   except :          model . data . result = RESULT_ERROR % str ( sys . exc_info ( ) [ : 2 ] )  finally :          job . service . save ( ) if repos :              for repo in repos :                  repo . destroy ( )      