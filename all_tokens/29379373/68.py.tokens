def install ( job ) :      cuisine = job . service . executor . cuisine name = <str> % job . service . name cuisine . apps . mongodb . install ( start = True , name = name )  def start ( job ) :      cuisine = job . service . executor . cuisine name = <str> % job . service . name cuisine . apps . mongodb . start ( name )  def stop ( job ) :      cuisine = job . service . executor . cuisine name = <str> % job . service . name cuisine . apps . mongodb . stop ( name )   