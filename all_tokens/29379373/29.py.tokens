def init_actions_ ( service , args ) :      return { <str> : [ <str> ] }  def test ( job ) :      import sys RESULT_OK = <str> RESULT_FAILED = <str> RESULT_ERROR = <str> % job . service . name model = job . service . model model . data . result = RESULT_OK % job . service . name try :          if job . service . name == <str> :              if not job . service . parent . name . startswith ( <str> ) :                  model . data . result = RESULT_FAILED % ( <str> )   if job . service . name == <str> :              if <str> in job . service . producers :                  model . data . result = RESULT_FAILED % ( <str> )   if job . service . name == <str> :              if <str> not in job . service . producers :                  model . data . result = RESULT_FAILED % ( <str> )  elif len ( job . service . producers [ <str> ] ) != 2 :                  model . data . result = RESULT_FAILED % ( <str> )    except :          model . data . result = RESULT_ERROR % str ( sys . exc_info ( ) [ : 2 ] )  job . service . save ( )   