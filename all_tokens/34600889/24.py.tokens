from django . conf import settings from constance import config import requests def send_message_android ( destination , message ) :      headers = { <str> : <str> + settings . FIREBASE_SERVER_KEY , <str> : <str> } payload = { <str> : destination , <str> : { <str> : config . TITLE_PUSH_NOTIFICATION , <str> : message } } request = requests . post ( settings . FIREBASE_API_URL , json = payload , headers = headers ) print ( request . text )  def send_message_ios ( destination , message ) :      headers = { <str> : <str> + settings . FIREBASE_SERVER_KEY , <str> : <str> } payload = { <str> : destination , <str> : <str> , <str> : 0 , <str> : { <str> : config . TITLE_PUSH_NOTIFICATION , <str> : message , <str> : <str> , } } request = requests . post ( settings . FIREBASE_API_URL , json = payload , headers = headers ) print ( request . text )  def send_push_notification ( to_user , message ) :      try :          devices = to_user . employeedevice_set . all ( ) if devices [ 0 ] . android_device :              send_message_android ( devices [ 0 ] . android_device , message )  if devices [ 0 ] . ios_device :              send_message_ios ( devices [ 0 ] . ios_device , message )  return True  except :          return False    