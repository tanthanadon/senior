from log import debug import xbmc def get_sources ( settings ) : 	 imdb_id = xbmc . getInfoLabel ( <str> ) title = xbmc . getInfoLabel ( <str> ) debug ( imdb_id ) debug ( title ) from service import add_media add_media ( title . decode ( <str> ) , imdb_id , settings )  if __name__ == <str> : 	 from player import load_settings settings = load_settings ( ) get_sources ( settings )    