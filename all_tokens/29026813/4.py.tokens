import requests , urllib from base import TorrentPlayer class ElementumPlayer ( TorrentPlayer ) : 	 def _log ( self , s ) : 		 from log import debug debug ( <str> + str ( s ) )  def close ( self ) : 		 pass  def StartBufferFile ( self , fileIndex ) : 		 self . fileIndex = fileIndex  def GetStreamURL ( self , playable_item ) : 		 index = playable_item . get ( <str> ) result = <str> . format ( urllib . quote ( self . magnet ( ) ) , index ) self . _log ( <str> + result ) return result  def magnet ( self ) : 		 td = self . GetLastTorrentData ( ) result = <str> . format ( td [ <str> ] , urllib . quote ( td [ <str> ] ) ) self . _log ( <str> + result ) return result    