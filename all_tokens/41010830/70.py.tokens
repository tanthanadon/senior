import sys import os import inspect from importlib import import_module def get_classes ( modName ) :      classNames = [ ] for name , obj in inspect . getmembers ( sys . modules [ modName ] ) :          if inspect . isclass ( obj ) :              classNames . append ( name )   return classNames  if __name__ == <str> :      destDir = sys . argv [ 1 ] modToDoc = sys . argv [ 2 ] import_module ( modToDoc ) if not os . path . exists ( destDir ) :          os . makedirs ( destDir )  for cls in get_classes ( modToDoc ) :          with open ( os . path . join ( destDir , <str> . format ( cls ) ) , <str> ) as F :              F . write ( . format ( cls = cls , ul = len ( cls ) * <str> , mod = modToDoc ) )     