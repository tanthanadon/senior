from setuptools import setup , find_packages import os import sys import re import shutil def get_version ( package ) :      init_py = open ( os . path . join ( package , <str> ) ) . read ( ) return re . search ( <str> , init_py ) . group ( 1 )  def read_all ( f ) :      with open ( f ) as I :          return I . read ( )   requirements = map ( str . strip , open ( <str> ) . readlines ( ) ) version = get_version ( <str> ) if sys . argv [ - 1 ] == <str> :      print ( <str> ) print ( <str> % ( version , version ) ) print ( <str> ) shutil . rmtree ( <str> ) shutil . rmtree ( <str> ) shutil . rmtree ( <str> ) sys . exit ( )  setup ( name = <str> , version = version , description = <str> , long_description = read_all ( <str> ) , classifiers = [ <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , ] , keywords = <str> , author = <str> , author_email = <str> , url = <str> , license = <str> , packages = find_packages ( exclude = [ <str> , <str> , <str> ] ) , test_suite = <str> , include_package_data = True , zip_safe = False , install_requires = requirements , entry_points = , )  