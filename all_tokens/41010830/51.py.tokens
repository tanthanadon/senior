import re import time from datetime import datetime def secs2time ( e ) :      w = time . gmtime ( e ) return datetime ( * list ( w ) [ 0 : 6 ] )  def granularity_to_time ( s ) :      mfact = { <str> : 1 , <str> : 60 , <str> : 3600 , <str> : 86400 , <str> : 604800 , } try :          f , n = re . match ( <str> , s ) . groups ( ) n = n if n else 1 return mfact [ f ] * int ( n )  except Exception as e :          raise ValueError ( e )    