import os import re import cjson fnLiblst = <str> def buildStdlibList ( ) :    liblst = [ <str> ] cwd_save = os . getcwd ( ) std_locations = ( <str> , <str> , <str> , <str> , <str> , <str> , ) for libdir in std_locations :      os . chdir ( libdir ) for lib in os . listdir ( <str> ) :        if re . match ( <str> , lib ) :          liblst . append ( lib )    os . chdir ( cwd_save ) with open ( fnLiblst , <str> ) as fd :      fd . write ( cjson . encode ( liblst ) ) fd . close ( )    