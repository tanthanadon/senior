from evdev import UInput , AbsInfo , InputDevice , categorize , ecodes , resolve_ecodes , resolve_ecodes_dict , AbsEvent , InputEvent import time from datetime import datetime from pymouse import PyMouse from pykeyboard import PyKeyboard from threading import Timer import subprocess def stamp ( ) : 	 stamp = datetime . now ( ) . time ( ) return stamp ;  def rightclick ( ) : 	 print <str> , pressed time . sleep ( 1 ) timestop = time . time ( ) x1 , y1 = m . position ( ) endx = x1 endy = y1 deltatime = timestop - timestart print <str> , pressed , <str> , deltatime , <str> , abs ( endx - startx ) , <str> , abs ( endy - starty ) m . press ( x1 , y1 , 2 ) m . release ( x1 , y1 , 2 )  dev = InputDevice ( <str> ) print ( dev ) m = PyMouse ( ) pressed = False for event in dev . read_loop ( ) : 	 if event . type == 1 and event . code == 330 and event . value == 1 : 		 x , y = m . position ( ) pressed = True timestart = time . time ( ) startx = x starty = y rightclick ( )    