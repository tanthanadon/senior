from evdev import InputDevice import time from pymouse import PyMouse from threading import Timer import subprocess def rightclick ( ) :      print <str> , pressed timestop = time . time ( ) endx , endy = m . position ( ) deltatime = timestop - timestart if pressed == True and ( abs ( endx - startx ) < 20 ) and ( abs ( endy - starty ) < 20 ) : 		 print <str> , pressed , <str> , deltatime , <str> , abs ( endx - startx ) , <str> , abs ( endy - starty ) subprocess . check_call ( [ <str> , <str> , <str> ] ) m . press ( endx , endy , 2 ) m . release ( endx , endy , 2 ) subprocess . check_call ( [ <str> , <str> , <str> ] )   dev = InputDevice ( <str> ) print ( dev ) m = PyMouse ( ) pressed = False for event in dev . read_loop ( ) :      if event . type == 1 and event . code == 330 and event . value == 1 :          startx , starty = m . position ( ) pressed = True timestart = time . time ( ) t = Timer ( 1.0 , rightclick ) t . start ( )  if event . type == 1 and event . code == 330 and event . value == 0 :          print <str> pressed = False t . cancel ( )  if event . type == 3 and event . code == 47 and event . value == 1 :          print <str> x2 , y2 = m . position ( ) m . click ( x2 , y2 , 2 ) print <str>    