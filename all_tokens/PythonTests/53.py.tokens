from evdev import UInput , AbsInfo from evdev import InputDevice , categorize , ecodes import time from datetime import datetime from pymouse import PyMouse from pykeyboard import PyKeyboard def stamp ( ) : 	 stamp = datetime . now ( ) . time ( ) return stamp ;  rep = 1 while rep == 1 : 	 print stamp ( ) , <str> time . sleep ( 2 ) print <str> time . sleep ( 5 ) m = PyMouse ( ) x , y = m . position ( ) m . move ( x + 10 , y + 10 ) m . click ( x + 20 , y + 20 , 2 ) print stamp ( ) , <str> , m . position ( )   