import sys import subprocess print <str> , len ( sys . argv ) print <str> , sys . argv validflags = 0 if len ( sys . argv ) > 1 : 	 for arg in sys . argv : 		 if arg == sys . argv [ 0 ] : 			 print <str>  elif arg == <str> : 			 print <str> validflags = validflags + 1  elif arg == <str> : 			 print <str> validflags = validflags + 1  else : 			 print arg , <str>    if len ( sys . argv ) == 1 or validflags == 0 : 		 print <str>   