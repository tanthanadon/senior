from openerp import models , fields , api , _ try :      from urllib import parse as urlparse  except :      from urlparse import urlparse  class AddGDriveWizard ( models . TransientModel ) :      _name = <str> name = fields . Char ( string = <str> ) url = fields . Char ( string = <str> ) @ api . model def action_add_gdrive ( self , name , url ) :          context = self . env . context if context is None :              context = { }  if not context . get ( <str> ) :              return  attachment_obj = self . pool [ <str> ] url = urlparse ( url ) if not url . scheme :              url = urlparse ( <str> % ( <str> , url ) )  for active_id in context . get ( <str> , [ ] ) :              attachment = { <str> : name , <str> : <str> , <str> : url . geturl ( ) , <str> : active_id , <str> : context [ <str> ] , } attachment_obj . create ( self . env . cr , context [ <str> ] , attachment , context = context )  return { <str> : <str> }    