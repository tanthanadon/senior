from openerp import models , fields , api import time from datetime import datetime from time import mktime class AccountInvoice ( models . Model ) :      _inherit = <str> @ api . one def _get_move_lines_str ( self ) :          expiration_dates_str = <str> move_line_obj = self . env [ <str> ] if self . move_id :              move_lines = move_line_obj . search ( [ ( <str> , <str> , self . move_id . id ) , ( <str> , <str> , False ) ] , order = <str> ) payment_mode = False if self . payment_mode_id :                  payment_mode = self . payment_mode_id expiration_dates_str += payment_mode . name + <str>  for line in move_lines :                  date = time . strptime ( line . date_maturity , <str> ) date = datetime . fromtimestamp ( mktime ( date ) ) date = date . strftime ( <str> ) expiration_dates_str += date + <str> + str ( self . type in ( <str> , <str> ) and line . debit or ( self . type in ( <str> , <str> ) and line . credit or 0 ) ) + <str>   self . expiration_dates_str = expiration_dates_str  expiration_dates_str = fields . Text ( <str> , readonly = True , compute = <str> )   