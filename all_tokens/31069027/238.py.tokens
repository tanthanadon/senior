from openerp import models , fields , api , exceptions , _ from datetime import datetime from openerp . tools import DEFAULT_SERVER_DATE_FORMAT as DATE_FORMAT class QwebWidgetDate ( models . AbstractModel ) :      _name = <str> _inherit = <str> def _format ( self , inner , options , qwebcontext ) :          inner = self . pool [ <str> ] . eval ( inner , qwebcontext ) inner_date = datetime . strptime ( inner , DATE_FORMAT ) . date ( ) lang_code = qwebcontext . context . get ( <str> ) or <str> lang_id = self . pool . get ( <str> ) . search ( qwebcontext . cr , qwebcontext . uid , [ ( <str> , <str> , lang_code ) ] ) date_format = self . pool . get ( <str> ) . read ( qwebcontext . cr , qwebcontext . uid , lang_id , [ <str> ] ) return inner_date . strftime ( date_format [ 0 ] [ <str> ] )   class QwebWidgetTime ( models . AbstractModel ) :      _name = <str> _inherit = <str> def _format ( self , inner , options , qwebcontext ) :          inner = self . pool [ <str> ] . eval ( inner , qwebcontext ) if isinstance ( inner , str ) :              inner = float ( inner )  return <str> % ( int ( inner ) , ( inner - int ( inner ) ) * 60 )    