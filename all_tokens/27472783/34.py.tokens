import getpass import sys from genestack_client import Application , GenestackException class SudoUtils ( Application ) :      APPLICATION_ID = <str> def is_sudo_active ( self ) :          return self . invoke ( <str> )  def ensure_sudo ( self , password ) :          return self . invoke ( <str> , password )  def ensure_sudo_interactive ( self , password ) :          if self . is_sudo_active ( ) :              return  if password is None :              while True :                  pwd = getpass . getpass ( <str> ) if self . ensure_sudo ( pwd ) :                      return  sys . stderr . write ( <str> )   elif not self . ensure_sudo ( password ) :              raise GenestackException ( <str> )     