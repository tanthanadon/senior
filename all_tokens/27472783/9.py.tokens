import imp import os from argparse import ArgumentParser template = def main ( ) :      with open ( os . path . join ( os . path . dirname ( __file__ ) , <str> , <str> , <str> , <str> , <str> ) ) as f :          module = imp . new_module ( <str> ) exec ( f . read ( ) , module . __dict__ )  name = <str> name_underline = <str> * len ( name ) parser = module . parser assert ( isinstance ( parser , ArgumentParser ) ) usage = parser . format_help ( ) . replace ( <str> , <str> ) . replace ( <str> , name ) with open ( <str> , <str> ) as f :          f . write ( template . format ( name = name , name_underline = name_underline , usage = usage ) )   if __name__ == <str> :      main ( )   