from __future__ import division , print_function import numpy as np def asymptotic_estimator ( p , sigp , floor = False ) :      p = np . asarray ( p ) sigp = np . asarray ( sigp ) if floor == True :          p_db = np . zeros ( len ( np . atleast_1d ( p ) ) ) p_db [ np . where ( p > sigp ) ] = np . sqrt ( p ** 2 - sigp ** 2 )  else :          p_db = np . sqrt ( p ** 2 - sigp ** 2 )  return p_db   