__author__ = [ <str> ] __license__ = <str> import re class JVReplacer ( object ) :      def __init__ ( self ) :          patterns = [ ( <str> , <str> ) , ( <str> , <str> ) , ( <str> , <str> ) , ( <str> , <str> ) ] self . patterns = [ ( re . compile ( regex ) , repl ) for ( regex , repl ) in patterns ]  def replace ( self , text ) :          for ( pattern , repl ) in self . patterns :              text = re . subn ( pattern , repl , text ) [ 0 ]  return text    