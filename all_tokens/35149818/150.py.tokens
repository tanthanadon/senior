class Solution ( object ) :      def isPalindrome ( self , x ) :          if x < 0 :              return False  if x <= 9 :              return True  high , low = 1 , 1 while high <= x :              high *= 10  high /= 10 while x :              high_digit = x / high low_digit = x % 10 if high_digit != low_digit :                  return False  x -= high_digit * high x /= 10 high /= 100  return True    