class TimePoint ( object ) :      def __init__ ( self , time , flag ) :          self . time = time self . flag = flag  def __cmp__ ( self , other ) :          if self . time != other . time :              return self . time - other . time  return self . flag - other . flag   class Solution :      def countOfAirplanes ( self , airplanes ) :          if not airplanes :              return 0  point_list = [ ] for a in airplanes :              point_list . append ( TimePoint ( a . start , 1 ) ) point_list . append ( TimePoint ( a . end , 0 ) )  point_list . sort ( ) cur_sum = 0 max_sum = float ( <str> ) for p in point_list :              if p . flag == 1 :                  cur_sum += 1  else :                  cur_sum -= 1  max_sum = max ( max_sum , cur_sum )  return max_sum    