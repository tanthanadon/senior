class Solution ( object ) :      def coinChange ( self , coins , amount ) :          if not coins or amount < 0 :              return - 1  if amount == 0 :              return 0  dp = [ float ( <str> ) ] * ( amount + 1 ) dp [ 0 ] = 0 for i in xrange ( 1 , len ( dp ) ) :              for j in coins :                  remain = float ( <str> ) if i - j < 0 else dp [ i - j ] dp [ i ] = min ( dp [ i ] , remain + 1 )   return dp [ - 1 ] if dp [ - 1 ] < float ( <str> ) else - 1    