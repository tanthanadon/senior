class Solution ( object ) :      def largestRectangleArea ( self , heights ) :          if not heights :              return 0  heights += [ 0 ] stack , result = [ ] , 0 for i in xrange ( len ( heights ) ) :              while stack and heights [ i ] < heights [ stack [ - 1 ] ] :                  last = stack . pop ( ) width = i - stack [ - 1 ] - 1 if stack else i result = max ( result , width * heights [ last ] )  stack . append ( i )  return result    