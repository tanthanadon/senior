class MovingAverage ( object ) :      def __init__ ( self , size ) :          self . window = size self . q = collections . deque ( ) self . cur_sum = 0  def next ( self , val ) :          self . cur_sum += val self . q . append ( val ) while len ( self . q ) > self . window :              popped = self . q . popleft ( ) self . cur_sum -= popped  return self . cur_sum * 1.0 / len ( self . q )    