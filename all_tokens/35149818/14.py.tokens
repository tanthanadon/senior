class Solution ( object ) :      def isValid ( self , s ) :          stack = [ ] for c in s :              if c in <str> :                  stack . append ( c )  else :                  if not stack : return False last = stack . pop ( ) if c == <str> and last != <str> : return False if c == <str> and last != <str> : return False if c == <str> and last != <str> : return False   return not stack    