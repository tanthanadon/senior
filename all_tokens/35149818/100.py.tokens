class Solution ( object ) :      def letterCombinations ( self , digits ) :          if not digits :              return [ ]  d = [ <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> , <str> ] q = collections . deque ( [ <str> ] ) for idx , val in enumerate ( digits ) :              while len ( q [ - 1 ] ) == idx :                  popped = q . pop ( ) for c in d [ int ( val ) ] :                      q . appendleft ( popped + c )    return list ( q )    