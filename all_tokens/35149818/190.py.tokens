class Solution ( object ) :      def fullJustify ( self , words , maxWidth ) :          if not words or maxWidth <= 0 :              return [ <str> ]  l , cur , result = 0 , [ ] , [ ] for w in words :              if l + len ( w ) + len ( cur ) > maxWidth :                  for i in xrange ( maxWidth - l ) :                      cur [ i % ( len ( cur ) - 1 or 1 ) ] += <str>  result . append ( <str> . join ( cur ) ) l , cur = 0 , [ ]  cur . append ( w ) l += len ( w )  last_line = <str> . join ( cur ) result . append ( last_line + ( <str> * ( maxWidth - len ( last_line ) ) ) ) return result    