__author__ = <str> import os import sys import pybedtools def run ( folder , genes ) :      for file1 in os . listdir ( folder ) :          if <str> in file1 and <str> not in file1 :              a = pybedtools . BedTool ( genes ) . sort ( ) b = pybedtools . BedTool ( folder + file1 ) . sort ( ) a . map ( b , 0 = <str> ) . saveas ( folder + file1 + <str> )    if __name__ == <str> :      folder = sys . argv [ 1 ] genes = <str> run ( folder , genes )   