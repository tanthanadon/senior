import tftpy import os import threading import time import logging DEFAULT_PORT = 50000 DEFAULT_ROOT = os . getcwd ( ) + <str> class Server :      def __init__ ( self , root_folder = DEFAULT_ROOT , port = DEFAULT_PORT ) :          self . server = tftpy . TftpServer ( root_folder ) self . port = port self . thread = threading . Thread ( target = self . server_listen )  def stop ( self , now = False ) :          self . server . stop ( now )  def start ( self ) :          self . thread . start ( )  def server_listen ( self ) :          self . server . listen ( listenport = self . port )  def is_running ( self ) :          return self . server . is_running . is_set ( )  @ staticmethod def set_logging ( write_path , level ) :          tftpy . setLogLevel ( level ) fh = logging . FileHandler ( write_path + <str> + time . strftime ( <str> ) ) formatter = logging . Formatter ( <str> ) fh . setFormatter ( formatter ) tftpy . log . addHandler ( fh )    