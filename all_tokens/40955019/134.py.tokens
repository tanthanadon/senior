import numpy as np import pandas as pd from jaratoolbox import celldatabase initialDB = <str> amDiscrimDB = <str> phaseDiscrimDB = <str> dbInitial = pd . read_hdf ( initialDB , key = <str> ) dbInitial [ <str> ] = dbInitial [ <str> ] . apply ( lambda x : [ x ] if not isinstance ( x , list ) else x ) dbAM = pd . read_hdf ( amDiscrimDB , key = <str> ) dbPhase = pd . read_hdf ( phaseDiscrimDB , key = <str> ) dbInitial [ <str> ] = dbAM [ <str> ] possibleRateKeys = np . array ( [ 4 , 5 , 8 , 11 , 16 , 22 , 32 , 45 , 64 , 90 , 128 ] ) for rate in possibleRateKeys :      dbInitial [ <str> . format ( rate ) ] = dbPhase [ <str> . format ( rate ) ]  newFn = <str> celldatabase . save_hdf ( dbInitial , newFn )  