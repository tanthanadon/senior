from __future__ import print_function import os from os . path import join , expanduser , exists from future . moves . urllib . request import urlopen from shutil import copyfileobj , rmtree from scipy import io import contextlib __all__ = ( <str> , <str> , <str> ) def get_data_dir ( ) :      base_odl_dir = os . environ . get ( <str> , expanduser ( join ( <str> , <str> ) ) ) data_home = join ( base_odl_dir , <str> ) if not exists ( data_home ) :          os . makedirs ( data_home )  return data_home  def cleanup_data_dir ( ) :      rmtree ( get_data_dir ( ) )  def get_data ( filename , subset , url ) :      data_dir = join ( get_data_dir ( ) , subset ) if not exists ( data_dir ) :          os . makedirs ( data_dir )  filename = join ( data_dir , filename ) if not exists ( filename ) :          print ( <str> <str> . format ( subset , filename , url ) ) with contextlib . closing ( urlopen ( url ) ) as data_url :              with open ( filename , <str> ) as storage_file :                  copyfileobj ( data_url , storage_file )    with open ( filename , <str> ) as storage_file :          data_dict = io . loadmat ( storage_file )  return data_dict  if __name__ == <str> :      from odl . util . testutils import run_doctests run_doctests ( )   