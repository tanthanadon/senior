from __future__ import print_function , division , absolute_import from itertools import product __all__ = ( <str> , ) def samples ( * sets ) :      if len ( sets ) == 1 :          for example in sets [ 0 ] . examples :              yield example   else :          generators = [ set_ . examples for set_ in sets ] for examples in product ( * generators ) :              yield examples    if __name__ == <str> :      from odl . util . testutils import run_doctests run_doctests ( )   