import os import unittest from cltk . corpus . swadesh import Swadesh __author__ = [ <str> , ] class TestCoptic ( unittest . TestCase ) :      def test_swadesh_coptic ( self ) :          swadesh = Swadesh ( <str> ) first_word = <str> match = swadesh . words ( ) [ 0 ] self . assertEqual ( first_word , match ) turn = [ <str> , <str> ] match = swadesh . words ( ) [ 125 ] self . assertEqual ( turn , match ) match = len ( swadesh . words ( ) ) self . assertEqual ( match , 207 )   if __name__ == <str> :      unittest . main ( )   