import smtplib from email . mime . text import MIMEText def process ( tr , parameters , tableBuilder ) :    fromState = str ( parameters . get ( <str> ) ) toState = str ( parameters . get ( <str> ) ) sampleCode = str ( parameters . get ( <str> ) ) server = <str> fromA = <str> toA = <str> subject = <str> % sampleCode text = <str> % ( <str> , <str> , sampleCode , <str> , fromState , <str> , toState , <str> , <str> , <str> ) msg = MIMEText ( text ) msg [ <str> ] = fromA msg [ <str> ] = toA msg [ <str> ] = subject msg [ <str> ] = <str> smtpServer = smtplib . SMTP ( server ) smtpServer . sendmail ( fromA , toA , msg . as_string ( ) ) smtpServer . close ( )   