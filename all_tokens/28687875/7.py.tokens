import os def process ( tr , parameters , tableBuilder ) :    file_name = parameters . get ( <str> ) lines = parameters . get ( <str> ) dropbox = <str> + parameters . get ( <str> ) + <str> f_path = os . path . join ( dropbox + file_name ) f = open ( f_path , <str> ) for line in lines : 	 f . write ( line + <str> )  f . close ( ) marker_path = os . path . join ( dropbox , <str> + file_name ) print f_path print marker_path marker = open ( marker_path , <str> ) marker . close ( )   