def process ( tr , parameters , tableBuilder ) :    import shutil import subprocess import os userid = parameters . get ( <str> ) dest_path = parameters . get ( <str> ) src_path = parameters . get ( <str> ) if os . path . isfile ( src_path ) :      shutil . copy ( src_path , dest_path )  else :      shutil . copytree ( src_path , dest_path )  cmd_line = [ ] cmd_line . append ( <str> ) cmd_line . append ( <str> ) param = <str> param = param + userid param = param + <str> cmd_line . append ( param ) cmd_line . append ( dest_path + os . path . basename ( src_path ) ) print cmd_line p = subprocess . Popen ( cmd_line , stdout = subprocess . PIPE ) stdout , stderr = p . communicate ( ) print stdout print stderr p2 = subprocess . Popen ( [ <str> , dest_path ] , stdout = subprocess . PIPE ) stdout2 , stderr2 = p2 . communicate ( ) print stdout2 print stderr2   