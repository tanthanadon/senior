import smtplib from email . mime . text import MIMEText def process ( tr , parameters , tableBuilder ) :    user = parameters . get ( <str> ) if not user == None :         tr . setUserId ( user )  expId = parameters . get ( <str> ) exp = tr . getExperimentForUpdate ( expId ) properties = parameters . get ( <str> ) for prop in properties . keySet ( ) :         exp . setPropertyValue ( prop , properties . get ( prop ) )    