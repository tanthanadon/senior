import smtplib from email . MIMEMultipart import MIMEMultipart from email . MIMEBase import MIMEBase from email . mime . text import MIMEText from email import Encoders def process ( tr , parameters , tableBuilder ) :    server = <str> fromA = <str> project = parameters . get ( <str> ) space = parameters . get ( <str> ) user = parameters . get ( <str> ) tsv = parameters . get ( <str> ) subject = user + <str> + project toA = <str> for mail in [ <str> , <str> ] :      toA += <str> % mail  text = <str> % ( user , project , space ) msg = MIMEMultipart ( ) msg [ <str> ] = fromA msg [ <str> ] = toA msg [ <str> ] = subject msg . attach ( MIMEText ( text ) ) part = MIMEBase ( <str> , <str> ) part . set_payload ( tsv ) Encoders . encode_base64 ( part ) part . add_header ( <str> , <str> % project + <str> ) msg . attach ( part ) smtpServer = smtplib . SMTP ( server ) smtpServer . sendmail ( fromA , toA , msg . as_string ( ) ) smtpServer . close ( )    